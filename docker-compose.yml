version: '3.3'
services:
    mariadb:
        image: mariadb
        restart: always
        ports:
        - 3308:3306
        environment:
            MYSQL_DATABASE: u1172916_default
            MYSQL_ROOT_PASSWORD: Ml7i_S4q
            MYSQL_USER: u1172916_default
            MYSQL_PASSWORD: Ml7i_S4q
        volumes:
            - ./Mariadb:/docker-entrypoint-initdb.d
            - ./DB:/var/lib/mysql
    api:
        build: ./api
        restart: always
        volumes: 
            - ./api/:/app
        ports:
            - 8000:80
    nginx:
        image: nginx:latest
        restart: always
        ports:
            - 8080:80
        volumes:
            - ./Nginx/core:/etc/nginx/conf.d
            - ./Nginx/www:/var/www/
            - ./Nginx/Logs:/var/log/nginx/
            - ./Nginx/html:/usr/share/nginx/html/
